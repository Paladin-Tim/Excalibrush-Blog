import {
  authorize,
  register,
  logout,
  fetchRoles,
  fetchUsers,
  updateUserRole,
  removeUser,
  fetchPosts,
  fetchPost,
  editPost,
  removePost,
  addComment,
  removeComment,
} from "./session-methods";

export const server = {
  authorize,
  register,
  logout,
  fetchRoles,
  fetchUsers,
  updateUserRole,
  removeUser,
  fetchPosts,
  fetchPost,
  editPost,
  removePost,
  addComment,
  removeComment,
};
