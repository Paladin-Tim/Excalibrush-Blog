import {
  authorize,
  register,
  logout,
  fetchRoles,
  fetchUsers,
  updateUserRole,
  removeUser,
} from "./session-methods";

export const server = {
  authorize,
  register,
  logout,
  fetchRoles,
  fetchUsers,
  updateUserRole,
  removeUser,
};
