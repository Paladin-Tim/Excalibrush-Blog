import {
  authorize,
  register,
  logout,
  fetchRoles,
  fetchUsers,
  updateUserRole,
  removeUser,
  fetchPost,
  addComment,
  removeComment,
} from "./session-methods";

export const server = {
  authorize,
  register,
  logout,
  fetchRoles,
  fetchUsers,
  updateUserRole,
  removeUser,
  fetchPost,
  addComment,
  removeComment,
};
